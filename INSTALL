How to install EasyPK
---------------------

You don't need to compile EasyPK because it's just a bunch of BASH scripts.
Having BASH installed is sufficient for using EasyPK.

To install EasyPK to your system, run as root:

# make install

To uninstall EasyPK, run as root:

# make uninstall


Note: As far as EasyPK is just a BASH scripts, you don't need to compile them -
just make sure you have BASH installed.

To install EasyPK to your system, run as root:

# make install

To uninstall EasyPK, run as root:

# make uninstall

